org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Tắt Kotlin incremental compilation - trên máy có project (D:) và pub cache (C:) nằm khác ổ đĩa,
# Kotlin compiler bị crash khi tính đường dẫn tương đối giữa 2 ổ khác nhau lúc đóng incremental
# cache (IllegalArgumentException: this and base files have different roots). Build release chậm
# hơn 1 chút nhưng không thường xuyên nên chấp nhận được.
kotlin.incremental=false
